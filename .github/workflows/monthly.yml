name: KOC 小群月报

on:
  schedule:
    - cron: '0 8 1 * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 20
    env:
      DISCORD_BOT_TOKEN: ${{ secrets.DISCORD_BOT_TOKEN }}
      ARK_API_KEY: ${{ secrets.ARK_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run Monthly Report
        working-directory: .github/scripts
        run: node report_monthly.js
      - name: Deploy HTML
        run: |
          set -euo pipefail
          SRC=".github/scripts/monthly-$(date +%Y%m%d).html"
          test -f "$SRC" || { echo "::error::expected $SRC to exist"; exit 1; }
          cp "$SRC" yoyo-community-report/monthly.html
          cp "$SRC" yoyo-community-report/
          git config user.name "Mochi Bot"
          git config user.email "bot@mochi.dev"
          git add yoyo-community-report/ .report-sent/
          git diff --staged --quiet || git commit -m "📊 Monthly report $(date +%Y%m%d)"
          git pull --rebase origin main && git push
